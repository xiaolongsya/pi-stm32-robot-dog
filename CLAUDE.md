# 机器狗 · CLAUDE.md

> 2026-09-01 立。桌面机器狗开源项目(大二学生作品)。
> 工作区根目录: `c:/Users/17402/Desktop/机器狗/`

---

## 项目一句话

桌面四足机器狗开源项目——平地走路 + 视觉跟踪 + 语音控制(不做跳跃)。

---

## 必说明

- 任何需要下载的依赖或者其他东西,都要优先选择清华源或者阿里源,都没法用再用官方源
- 务必诚实回答,如搜索失败、没有这方面知识,应如是告知用户
- 项目进度会不断推进,有新进度或者改动后记得更新 README.md 等进度文档
- 必须先了解清楚当前的项目结构与功能后才能执行任务
- 无论是否完成任务,**都不得擅自做决定**——提新想法/顺便完成都必须经过用户同意
- 修复 bug 时,尽量每修完一个就提交一次
- **Git 自动提交规则(2026-09-02 立):每完成一个大任务(琐碎的合并提交)就自动 `git add` + `git commit`,无需用户确认**。只在以下情况停下来:`git push` 前要确认(避免推到公共仓库造成混乱)、涉及不可逆操作(`git reset --hard`、`git clean -fd` 等)、冲突解决
- **语言规则(2026-09-02 立):尽量用中文跟用户交流,包括整个任务过程;专业词汇(芯片型号、引脚名、网络名如 I2C_SDA、协议术语如 PWM 等)用英文没问题**

---

## 拍板暂停规则

- **遇到需要用户拍板/决策的事项时,必须立刻停下来等待**,不要继续推进任务或说"下一步做什么"
- 列出待拍板事项 + 清晰给出选项,等用户回复
- 用户处理完所有拍板后,再推进下一步
- 不要混进"下一步做什么"等其他内容,不然很乱

---

## PCB 画板坐标规则

- **设置坐标后必须检查是否符合分区规划**,不能"中心"万金油
- 坐标必须明确说明位于 **板左/板右/板上/板下/中心**
- 坐标计算时考虑元件实际尺寸占空间,**画 ASCII 草图验证** 再给出
- 给出坐标前自检 3 项:
  1. 该坐标在哪个分区?
  2. 占空间多大?(X 范围 / Y 范围)
  3. 周围是否合理?会不会留白或冲突
- 板子坐标系:Y 轴向上,左下角 (0,0),右上角 (100,100)
- 板子 4 螺丝孔固定位置:(5,5) (95,5) (5,95) (95,95) — 板上其他结构不要覆盖
- 涉及 PCB 板布局变更,必须先 **画 ASCII 草图** 给用户确认,再画
- **电路不允许推测**:没查到的数据,要求用户提供数据手册或者自己查 PDF/官方资料,**不准猜**

---

## PCB v3 工作流(必读)

- **先画原理图 → ERC 0 错误 0 警告 → 导网表 → 核对关键网络 → PCB → DRC**
- **参考图为准,不质疑参考图**——用户选定嘉立创开源参考图为标准
- **对照数据手册典型应用图 + 参考图逐引脚核对**(电源/地/自举/补偿/反馈/使能/续流都不能漏)
- **异步降压必加续流二极管;同步降压不需要**
- **整体规划前置**:走线 + 芯片选型 + 散热 + EMC 在原理图阶段一起看
- **分区 → 内层电压 → 走线 → EMC** 同步规划(顺序反了返工)
- **散热前置**:大功率 IC 满载功耗 → 散热片选型 → 风扇 CFM(散热片/风扇位留到画完板再想就晚了)
- **建模三道关**:卡尺测 → 反向验算 → 3D 装配 → 实测(只测一遍就出图必翻车)
- **选型前先算功率/电压/体积**:含负载峰值电流核算(如舵机堵转峰值 vs 降压 IC 限流)

---

## 香橙派 Zero3 注意事项(若沿用)

- **I2S 不可用**:Pi Zero3 26-pin 接口实测 I2S 出问题(love-bot 经验,改用 USB 声卡 CM108 兜底)
- **老内核限制**:H616 内核时钟配置问题 → 音频采样率可能减半(48kHz 流实测 0.5x 播放)
- **音频修复方案**:见 `love-bot-body/deploy/audio-fix/README.md`,`devmem2` 100ms 巡检纠错
- **板载蓝牙 vs USB**:若用蓝牙,需 blacklist 内核模块 `sprdbt_tty`,只保留 USB 发射器作 hci0
- **软 I2C workaround**:H616 上若 I2C 总线驱动不稳,用 PH4/PH5 i2c-gpio bit-bang(见 `love-bot-body/robot-pcb-v2/i2c-gpio-overlay.dts`)

---

## STM32G431KBT6 协处理器架构(2026-09-01 拍板)

- **型号**:STM32G431KBT6(**不是 F411**)
- **封装**:LQFP-32(7×7mm,**0.8mm pitch**,比 F411 LQFP-64 容易焊)
- **理由**:170MHz Cortex-M4F + 8 路 PWM 够用 + 库存好
- **角色**:香橙派 Zero3 + STM32 双 MCU 协作
  - 香橙派:视觉/语音/高层决策(计算量大,不实时)
  - **STM32G431:实时舵机控制 + IMU 1000 Hz 闭环 + 8 路 PWM 直接驱动**(无 PCA9685)
- **通信**:UART(USART1,PA9/PA10)+ 排针 J1.7/J1.8 引出
- **Pi 供电**:独立 XH2.54 2P 母座 CN2,经 LM2596 #2(5V_LOGIC 母线)
- **舵机**:8 路 MG90S,直接连 STM32 PA2~PA7 + PB0 + PB3(无 PCA9685)
- **I2C 总线**:MPU6050(IMU)+ SSD1306(OLED),经 STM32 PB6(SDA)+ PA15(SCL)
- **OLED SSD1306**:0.96 寸 I2C,XH2.54 4P 母座,VCC/GND/SCL/SDA
- **电池**:2S 18650 6800mAh(蓝火新能源,瞬时 6~8A,持续 3~4A)
- **不画原理图禁止画 PCB**(love-bot v1 死法)

---

## v3 已删的设计

| 删除项 | 原因 |
|---|---|
| ~~Hi3861(原 U1 协处理器)~~ | 用 STM32G431 替代,功能更强 |
| ~~PCA9685(原 U2 16 路 PWM)~~ | STM32 直接出 8 路 PWM,省 I2C 设备 |
| ~~IP2325(充电 IC)~~ | 不做充电,拆电池外充 |
| ~~TDA7297(音频功放)~~ | 用 USB 声卡替代 |
| ~~SS14 × 2 ORing 给 5V 总线~~ | 用户撤了(走线更简单) |
| ~~5V 拆分为 5V_SERVO / 5V_LOGIC~~ | 用户撤了(Pi 独立供电就够了) |
| ~~Hi3861+PCA9685 参考网表~~ | 此文件不存在,架构已变 |

---

## 文件结构

```
机器狗/
├── CLAUDE.md              # 本文件(规则)
├── README.md              # 项目说明 + 范围 + 架构
├── KICKOFF_PROMPT.md      # 新对话启动提示词
├── 网表/                  # v3 网表(Netlist_控制板_2026-09-01.tel)
├── 数据手册/              # v3 数据手册(STM32G431KBT6.pdf + MPU6050.pdf)
├── v2-BOM/                # 从 love-bot项目 迁移的 v2 BOM(参考)
├── v2-网表/               # 从 love-bot项目 迁移的 v2 网表(参考)
├── v2-gerber/             # 从 love-bot项目 迁移的 v2 gerber(参考)
├── 数据手册-参考/         # 从 love-bot项目 迁移的 IC 数据手册(参考,仅作对比)
├── v3-sch/                # v3 原理图(待创建)
├── v3-pcb/                # v3 PCB(待创建)
└── ...
```

---

## 关键参考文件

- [`README.md`](README.md) — 项目说明、范围、硬件架构、v3 PCB 决策
- [`KICKOFF_PROMPT.md`](KICKOFF_PROMPT.md) — 新对话启动提示词
- `../love-bot项目/love-bot-body/三战总结.md` — 三战经验沉淀(v3 必读)
- `../love-bot项目/love-bot-body/robot-pcb-v2/STOP.md` — v2 PCB 终止说明 + 教训清单
- `../love-bot项目/love-bot-body/robot-pcb-v2/sch-power.md` — v2 充电/降压/功放电源拓扑(思路参考)
- `../love-bot项目/love-bot-body/robot-pcb-v2/sch-protection.md` — AO3401A 防反接通用方案
- `../love-bot项目/love-bot-body/deploy/audio-fix/README.md` — H616 音频修复(若沿用 Pi Zero3)

---

## 工作流

1. 修改工作区文件 → 在 `机器狗/` 目录下提交(本目录暂未初始化 git,等用户拍板)
2. 涉及 PCB 板变更 → 先画 ASCII 草图给用户确认
3. 拍板事项 → 停下来等用户回复,不擅自决定